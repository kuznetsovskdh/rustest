

      // ─── ОБЛОЖКА ─────────────────────────────────────────────────────────────
      new Paragraph({
        alignment: AlignmentType.CENTER,
        spacing: { before: 2000, after: 200 },
        children: [new TextRun({ text: "СПРАВОЧНИК ДЛЯ ПОДГОТОВКИ К СОБЕСЕДОВАНИЮ", bold: true, size: 52, font: "Arial", color: BLUE })]
      }),
      new Paragraph({
        alignment: AlignmentType.CENTER,
        spacing: { before: 0, after: 200 },
        children: [new TextRun({ text: "Аналитик данных / Продуктовый аналитик", size: 30, font: "Arial", color: LIGHT_BLUE })]
      }),
      new Paragraph({
        alignment: AlignmentType.CENTER,
        spacing: { before: 0, after: 3000 },
        children: [new TextRun({ text: "SQL · Python · Excel · Статистика · Продуктовая аналитика", size: 24, font: "Arial", color: "666666", italics: true })]
      }),
      pageBreak(),

      // ══════════════════════════════════════════════════════════════════════════
      // 1. БАЗЫ ДАННЫХ
      // ══════════════════════════════════════════════════════════════════════════
      h1("1. БАЗЫ ДАННЫХ — ТЕОРИЯ"),
      spacer(),

      h2("1.1 Что такое база данных"),
      p("База данных — это организованное хранилище структурированной информации, которое позволяет эффективно хранить, искать и извлекать данные. Данные хранятся на диске и управляются специальным программным обеспечением — СУБД (системой управления базами данных)."),
      spacer(),

      h2("1.2 Виды баз данных"),
      h3("Реляционные (SQL)"),
      p("Хранят данные в виде таблиц (строки и столбцы), связанных между собой. Используют язык SQL для запросов. Примеры: PostgreSQL, MySQL, ClickHouse."),
      bullet("PostgreSQL — мощная open-source СУБД, стандарт в аналитике"),
      bullet("MySQL — часто используется в веб-приложениях"),
      bullet("ClickHouse — колоночная СУБД для аналитических нагрузок (OLAP)"),
      spacer(),

      h3("Нереляционные (NoSQL)"),
      p("Данные хранятся без жёсткой схемы — документы, ключ-значение, графы. Примеры: MongoDB, Redis, Cassandra. Используются там, где нужна гибкость структуры или огромные объёмы."),
      spacer(),

      h2("1.3 Ключевые понятия реляционной БД"),
      h3("Первичный ключ (Primary Key)"),
      p("Столбец (или набор столбцов), который уникально идентифицирует каждую строку таблицы. Не может быть NULL, значения не повторяются. Пример: user_id в таблице пользователей."),
      spacer(),

      h3("Внешний ключ (Foreign Key)"),
      p("Столбец, который ссылается на первичный ключ другой таблицы. Обеспечивает целостность данных: нельзя добавить запись с несуществующей ссылкой. Пример: order_id в таблице заказов ссылается на user_id в таблице пользователей."),
      spacer(),

      h3("Связи между таблицами"),
      bullet("One-to-One (один-к-одному) — пользователь и его профиль"),
      bullet("One-to-Many (один-ко-многим) — пользователь и его заказы"),
      bullet("Many-to-Many (многие-ко-многим) — студенты и курсы; реализуется через таблицу-посредник"),
      spacer(),

      h3("NULL"),
      p("NULL — это отсутствие значения, не ноль и не пустая строка. С NULL нельзя делать обычные сравнения (WHERE col = NULL не работает). Нужно использовать IS NULL / IS NOT NULL."),
      spacer(),

      h2("1.4 Типы данных"),
      bullet("Числа: INTEGER, BIGINT, FLOAT, DECIMAL(p,s)"),
      bullet("Строки: VARCHAR(n), TEXT"),
      bullet("Даты и время: DATE, TIMESTAMP, INTERVAL"),
      bullet("Логические: BOOLEAN (TRUE / FALSE)"),
      bullet("JSON: текстовый формат обмена данными, хранит вложенные структуры"),
      spacer(),

      h2("1.5 Архитектура данных"),
      h3("OLTP vs OLAP"),
      pMixed([run("OLTP (Online Transaction Processing) — ", { bold: true }), run("оперативные БД, обслуживающие приложения. Много коротких транзакций (вставка, обновление). Примеры: PostgreSQL, MySQL.")]),
      pMixed([run("OLAP (Online Analytical Processing) — ", { bold: true }), run("аналитические БД. Мало запросов, каждый тяжёлый (агрегации по миллионам строк). Часто колоночные. Пример: ClickHouse.")]),
      spacer(),

      h3("Слои данных (Data Layers)"),
      bullet("Raw — сырые данные как есть, без изменений"),
      bullet("Staging — очищенные данные, приведённые к единому формату"),
      bullet("Marts — агрегированные таблицы под конкретные задачи бизнеса"),
      spacer(),

      h3("Подходы к обработке данных"),
      bullet("ETL (Extract → Transform → Load) — трансформируем до загрузки в хранилище"),
      bullet("ELT (Extract → Load → Transform) — грузим сырые данные, трансформируем уже в хранилище"),
      bullet("CDC (Change Data Capture) — ловим только изменения, а не перегружаем всё каждый раз. Сложнее настроить, но эффективнее."),
      spacer(),

      h3("Нормализация и денормализация"),
      p("Нормализация — правила разбивки данных по таблицам так, чтобы каждый факт хранился в одном месте. Уменьшает дублирование, упрощает обновление. Денормализация — сознательное объединение данных для ускорения аналитических запросов."),
      spacer(),

      h3("Индексы"),
      p("Индекс — структура данных, ускоряющая поиск строк в таблице. Работает как содержание книги. Важно: большое количество индексов замедляет запись, ускоряет чтение."),
      spacer(),

      h3("Партиционирование"),
      p("Разбиение большой таблицы на физические части (партиции), обычно по дате. Позволяет читать только нужные партиции, а не всю таблицу."),
      spacer(),

      h3("Репликация"),
      p("Репликация — создание копии БД, которая синхронизируется с основной с небольшой задержкой. Используется для отказоустойчивости и распределения нагрузки на чтение."),
      spacer(),

      h3("Инструменты аналитического стека"),
      bullet("dbt — инструмент для описания трансформаций данных на SQL. Каждый .sql файл — одна таблица, dbt сам строит граф зависимостей и запускает всё в нужном порядке."),
      bullet("Airflow — оркестратор задач. Позволяет описывать пайплайны (DAG) и запускать их по расписанию."),
      bullet("Git — система контроля версий кода. GitHub — платформа для хранения репозиториев."),
      spacer(),

      pageBreak(),

      // ══════════════════════════════════════════════════════════════════════════
      // 2. SQL
      // ══════════════════════════════════════════════════════════════════════════
      h1("2. SQL"),
      spacer(),

      h2("2.1 Общая структура запроса"),
      p("Запрос всегда выполняется в определённом порядке, который отличается от порядка написания:"),
      bullet("Порядок написания: SELECT → FROM → JOIN → WHERE → GROUP BY → HAVING → ORDER BY → LIMIT"),
      bullet("Порядок выполнения: FROM → JOIN → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT"),
      note("На собеседовании часто спрашивают: «Почему нельзя использовать алиас из SELECT в WHERE?» — потому что WHERE выполняется до SELECT."),
      spacer(),

      h2("2.2 Полный синтаксис запроса"),
      code("WITH cte1 AS ("),
      code("    SELECT ..."),
      code("),"),
      code("cte2 AS ("),
      code("    SELECT ..."),
      code(")"),
      code("SELECT"),
      code("    a.col1,"),
      code("    b.col2,"),
      code("    SUM(a.amount) OVER (PARTITION BY a.user_id ORDER BY a.date) AS running_sum,"),
      code("    CASE"),
      code("        WHEN a.status = 'active' THEN 'Активный'"),
      code("        WHEN a.status = 'churned' THEN 'Отток'"),
      code("        ELSE 'Прочее'"),
      code("    END AS status_label"),
      code("FROM table1 AS a"),
      code("LEFT JOIN table2 AS b ON a.id = b.id"),
      code("WHERE a.amount > (SELECT AVG(amount) FROM table1)"),
      code("GROUP BY a.col1, b.col2"),
      code("HAVING SUM(a.amount) > 1000"),
      code("ORDER BY running_sum DESC"),
      code("LIMIT 100;"),
      spacer(),

      h2("2.3 JOIN — соединение таблиц"),
      h3("Виды JOIN"),
      bullet("INNER JOIN — только совпадающие строки в обеих таблицах"),
      bullet("LEFT JOIN — все строки из левой + совпадения из правой (NULL если нет)"),
      bullet("RIGHT JOIN — все строки из правой + совпадения из левой"),
      bullet("FULL OUTER JOIN — все строки из обеих таблиц"),
      bullet("CROSS JOIN — декартово произведение: каждая строка левой с каждой строкой правой"),
      bullet("SELF JOIN — таблица джойнится сама на себя (например, найти начальника сотрудника)"),
      note("Самый частый на практике — LEFT JOIN. На собеседовании обязательно объясните разницу LEFT vs INNER."),
      spacer(),

      h3("Пример: найти пользователей без заказов"),
      code("SELECT u.user_id, u.name"),
      code("FROM users u"),
      code("LEFT JOIN orders o ON u.user_id = o.user_id"),
      code("WHERE o.user_id IS NULL;"),
      spacer(),

      h2("2.4 Агрегатные функции"),
      bullet("COUNT(*) — количество строк"),
      bullet("COUNT(col) — количество не-NULL значений"),
      bullet("COUNT(DISTINCT col) — количество уникальных значений"),
      bullet("SUM(col) — сумма"),
      bullet("AVG(col) — среднее"),
      bullet("MIN(col) / MAX(col) — минимум / максимум"),
      spacer(),

      h2("2.5 UNION и UNION ALL"),
      pMixed([run("UNION ", { bold: true }), run("— объединяет результаты двух запросов и удаляет дубликаты строк (медленнее).")]),
      pMixed([run("UNION ALL ", { bold: true }), run("— объединяет результаты, сохраняя все строки включая дубликаты (быстрее, используйте его если дубликаты не важны).")]),
      spacer(),

      h2("2.6 Оконные функции"),
      p("Оконные функции выполняют вычисления по набору строк, связанных с текущей строкой, не сворачивая результат. В отличие от GROUP BY, строки остаются видны."),
      spacer(),

      h3("Синтаксис"),
      code("функция() OVER ("),
      code("    PARTITION BY col1   -- разбиваем на окна"),
      code("    ORDER BY col2       -- сортировка внутри окна"),
      code("    ROWS BETWEEN x PRECEDING AND y FOLLOWING  -- фрейм"),
      code(")"),
      spacer(),

      h3("Агрегации в окне"),
      bullet("SUM(amount) OVER (PARTITION BY user_id ORDER BY date) — нарастающая сумма"),
      bullet("AVG, MIN, MAX, COUNT — аналогично"),
      spacer(),

      h3("Смещение"),
      bullet("LAG(col, n) OVER (...) — значение из предыдущей строки (n шагов назад)"),
      bullet("LEAD(col, n) OVER (...) — значение из следующей строки (n шагов вперёд)"),
      bullet("FIRST_VALUE(col) OVER (...) — первое значение в окне"),
      bullet("LAST_VALUE(col) OVER (...) — последнее значение в окне"),
      spacer(),

      h3("Ранжирование"),
      bullet("ROW_NUMBER() — уникальный номер каждой строки, дубликаты не влияют"),
      bullet("RANK() — при дубликатах одинаковый ранг, следующий номер пропускается (1, 1, 3)"),
      bullet("DENSE_RANK() — при дубликатах одинаковый ранг, пропусков нет (1, 1, 2)"),
      bullet("NTILE(n) — равномерно делит строки на n групп"),
      spacer(),

      h3("Пример: найти топ-1 заказ каждого пользователя"),
      code("SELECT user_id, order_id, amount"),
      code("FROM ("),
      code("    SELECT user_id, order_id, amount,"),
      code("           ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY amount DESC) AS rn"),
      code("    FROM orders"),
      code(") t"),
      code("WHERE rn = 1;"),
      spacer(),

      h2("2.7 Работа с датами (PostgreSQL)"),
      bullet("DATE_TRUNC('week', event_date) — обрезает дату до начала недели"),
      bullet("DATE_TRUNC('month', event_date) — обрезает до начала месяца"),
      bullet("DATE_PART('month', date) — извлекает номер месяца (1–12)"),
      bullet("DATE_PART('dow', date) — день недели (0 = воскресенье)"),
      bullet("NOW() — текущая дата и время"),
      bullet("CURRENT_DATE — текущая дата"),
      bullet("date + INTERVAL '7 days' — добавить интервал"),
      bullet("date1 - date2 — разница дат в днях"),
      bullet("EXTRACT(EPOCH FROM interval) — интервал в секундах"),
      bullet("AGE(date_end, date_start) — возвращает '2 года 3 месяца 10 дней'"),
      spacer(),

      h2("2.8 Фильтрация"),
      bullet("LIKE '%text%' — поиск подстроки (% = любые символы, _ = один символ)"),
      bullet("IN (val1, val2) — значение из списка"),
      bullet("BETWEEN a AND b — значение в диапазоне (включительно)"),
      bullet("IS NULL / IS NOT NULL — проверка на NULL"),
      spacer(),

      h2("2.9 Подзапросы и CTE"),
      pMixed([run("CTE (Common Table Expression) ", { bold: true }), run("— именованный подзапрос через WITH. Делает запрос читаемым, можно ссылаться несколько раз.")]),
      code("WITH active_users AS ("),
      code("    SELECT user_id FROM users WHERE status = 'active'"),
      code(")"),
      code("SELECT COUNT(*) FROM orders WHERE user_id IN (SELECT user_id FROM active_users);"),
      spacer(),

      h2("2.10 Продвинутые темы SQL"),
      bullet("EXPLAIN / EXPLAIN ANALYZE — план выполнения запроса, помогает найти узкие места"),
      bullet("Рекурсивные CTE — для обхода иерархических структур (дерево менеджеров)"),
      bullet("Материализованные представления (Materialized View) — результат запроса сохраняется на диск"),
      bullet("Работа с JSON в SQL — операторы ->, ->>, jsonb_array_elements"),
      bullet("Шардирование — горизонтальное разбиение данных по разным серверам"),
      spacer(),

      pageBreak(),

      // ══════════════════════════════════════════════════════════════════════════
      // 3. EXCEL
      // ══════════════════════════════════════════════════════════════════════════
      h1("3. EXCEL — ФОРМУЛЫ И ПРИЁМЫ"),
      spacer(),
      p("Excel остаётся обязательным инструментом аналитика для быстрого исследования данных, отчётности и коммуникации с бизнесом. Ключевые темы: подтягивание данных из другой таблицы, условная агрегация, логика."),
      spacer(),

      h2("3.1 ВПР (VLOOKUP) — вертикальный поиск"),
      p("Ищет значение в первом столбце таблицы и возвращает значение из указанного столбца той же строки."),
      code("=ВПР(искомое_значение; таблица; номер_столбца; [интервальный_просмотр])"),
      spacer(),
      bullet("искомое_значение — что ищем (например, ID)"),
      bullet("таблица — диапазон поиска, начиная с ключевого столбца"),
      bullet("номер_столбца — из какого столбца возвращать результат (1 = первый)"),
      bullet("интервальный_просмотр — ЛОЖЬ (точное совпадение) или ИСТИНА (приближённое)"),
      spacer(),
      p("Пример: подтянуть имя пользователя по его ID:"),
      code("=ВПР(A2; Пользователи!$A:$B; 2; ЛОЖЬ)"),
      note("ВПР ищет только вправо. Ключевой столбец должен быть крайним левым. Для более гибкого поиска используйте ИНДЕКС+ПОИСКПОЗ."),
      spacer(),

      h2("3.2 ГПР (HLOOKUP) — горизонтальный поиск"),
      p("Аналог ВПР, но ищет по строкам (горизонтально)."),
      code("=ГПР(искомое_значение; таблица; номер_строки; [интервальный_просмотр])"),
      spacer(),

      h2("3.3 ИНДЕКС + ПОИСКПОЗ — гибкий поиск"),
      p("Комбинация этих двух функций — самый мощный способ подтягивания данных. В отличие от ВПР, может искать в любом направлении и не зависит от порядка столбцов."),
      spacer(),
      pMixed([run("ПОИСКПОЗ ", { bold: true }), run("находит позицию (номер строки/столбца) искомого значения в массиве:")]),
      code("=ПОИСКПОЗ(искомое_значение; просматриваемый_массив; [тип_сопоставления])"),
      bullet("тип_сопоставления: 0 = точное совпадение, 1 = наибольшее ≤ значению, -1 = наименьшее ≥ значению"),
      spacer(),
      pMixed([run("ИНДЕКС ", { bold: true }), run("возвращает значение из массива по номеру строки и/или столбца:")]),
      code("=ИНДЕКС(массив; номер_строки; [номер_столбца])"),
      spacer(),
      p("Пример — найти имя по ID (аналог ВПР, но гибче):"),
      code("=ИНДЕКС(B:B; ПОИСКПОЗ(A2; A:A; 0))"),
      tip("Связка ИНДЕКС+ПОИСКПОЗ предпочтительнее ВПР: работает быстрее на больших данных, не ломается при добавлении столбцов."),
      spacer(),

      h2("3.4 ЕСЛИ (IF) — логика"),
      p("Проверяет условие и возвращает одно из двух значений."),
      code("=ЕСЛИ(логическое_выражение; значение_если_истина; [значение_если_ложь])"),
      spacer(),
      p("Пример — категоризация суммы:"),
      code("=ЕСЛИ(B2>10000; \"Крупный\"; ЕСЛИ(B2>1000; \"Средний\"; \"Мелкий\"))"),
      bullet("Вложенные ЕСЛИ можно использовать, но при более 3 уровней лучше использовать ЕСЛИМН"),
      spacer(),
      pMixed([run("ЕСЛИМН ", { bold: true }), run("(IFS) — множественные условия без вложений:")]),
      code("=ЕСЛИМН(B2>10000; \"Крупный\"; B2>1000; \"Средний\"; ИСТИНА; \"Мелкий\")"),
      spacer(),

      h2("3.5 СУММЕСЛИ и СУММЕСЛИМН"),
      pMixed([run("СУММЕСЛИ ", { bold: true }), run("— суммирует значения, если выполняется одно условие:")]),
      code("=СУММЕСЛИ(диапазон_условия; условие; [диапазон_суммирования])"),
      spacer(),
      p("Пример — сумма продаж по региону «Москва»:"),
      code("=СУММЕСЛИ(C:C; \"Москва\"; D:D)"),
      spacer(),
      pMixed([run("СУММЕСЛИМН ", { bold: true }), run("— суммирует при выполнении нескольких условий:")]),
      code("=СУММЕСЛИМН(диапазон_суммирования; диапазон_условия1; условие1; диапазон_условия2; условие2; ...)"),
      spacer(),
      p("Пример — сумма продаж в регионе «Москва» за категорию «Электроника»:"),
      code("=СУММЕСЛИМН(D:D; C:C; \"Москва\"; B:B; \"Электроника\")"),
      spacer(),

      h2("3.6 СЧЁТЕСЛИ и СЧЁТЕСЛИМН"),
      pMixed([run("СЧЁТЕСЛИ ", { bold: true }), run("— подсчитывает количество ячеек, удовлетворяющих условию:")]),
      code("=СЧЁТЕСЛИ(диапазон; условие)"),
      spacer(),
      p("Пример — сколько заказов со статусом «Доставлен»:"),
      code("=СЧЁТЕСЛИ(A:A; \"Доставлен\")"),
      spacer(),
      pMixed([run("СЧЁТЕСЛИМН ", { bold: true }), run("— подсчёт при нескольких условиях:")]),
      code("=СЧЁТЕСЛИМН(диапазон_условия1; условие1; диапазон_условия2; условие2; ...)"),
      spacer(),

      h2("3.7 Сводные таблицы (Pivot Table)"),
      p("Сводная таблица — интерактивный инструмент для быстрой группировки, агрегации и анализа больших наборов данных без написания формул. Позволяет за секунды получить суммы, средние, количества в разрезе любого поля."),
      bullet("Выделите данные → Вставка → Сводная таблица"),
      bullet("Перетащите поля в области: Строки, Столбцы, Значения, Фильтры"),
      bullet("Двойной клик на значение — развернуть исходные данные"),
      tip("Pivot Table — главный Excel-навык для аналитика. Практикуйтесь строить их на реальных данных быстро."),
      spacer(),

      h2("3.8 Другие полезные функции"),
      bullet("ЕСЛИОШИБКА(формула; значение_при_ошибке) — обработка ошибок, например =ЕСЛИОШИБКА(ВПР(...); 0)"),
      bullet("И(условие1; условие2) / ИЛИ(условие1; условие2) — логические операторы"),
      bullet("СЦЕП(текст1; текст2) или & — объединение строк"),
      bullet("ЛЕВСИМВ / ПРАВСИМВ / ПСТР — работа с подстроками"),
      bullet("ТЕКСТ(число; формат) — форматирование числа как текста"),
      spacer(),

      pageBreak(),

      // ══════════════════════════════════════════════════════════════════════════
      // 4. PYTHON
      // ══════════════════════════════════════════════════════════════════════════
      h1("4. PYTHON ДЛЯ АНАЛИТИКИ"),
      spacer(),

      h2("4.1 Базовые типы данных"),
      bullet("int — целые числа: 42, -7"),
      bullet("float — числа с плавающей точкой: 3.14, -0.5"),
      bullet("str — строки: 'hello', \"world\""),
      bullet("bool — булевы значения: True, False"),
      bullet("None — отсутствие значения (аналог NULL в SQL)"),
      spacer(),

      h2("4.2 Структуры данных"),
      h3("Список (list) — изменяемый, упорядоченный"),
      code("nums = [1, 2, 3, 4]"),
      code("nums.append(5)     # добавить в конец"),
      code("nums[0]            # первый элемент = 1"),
      code("nums[-1]           # последний элемент = 5"),
      code("nums[1:3]          # срез: [2, 3]"),
      spacer(),

      h3("Кортеж (tuple) — неизменяемый, упорядоченный"),
      code("point = (10, 20)"),
      code("x, y = point       # распаковка"),
      spacer(),

      h3("Словарь (dict) — ключ: значение"),
      code("user = {'id': 1, 'name': 'Анна', 'age': 28}"),
      code("user['name']       # 'Анна'"),
      code("user.get('email', 'N/A')  # безопасное получение"),
      code("user.keys()        # dict_keys(['id', 'name', 'age'])"),
      code("user.values()      # dict_values([1, 'Анна', 28])"),
      code("user.items()       # пары (ключ, значение)"),
      spacer(),

      h3("Множество (set) — уникальные значения"),
      code("s = {1, 2, 3, 2, 1}  # {1, 2, 3}"),
      code("s.add(4)"),
      code("s1 & s2  # пересечение"),
      code("s1 | s2  # объединение"),
      code("s1 - s2  # разность"),
      spacer(),

      h2("4.3 Управляющие конструкции"),
      h3("Условия"),
      code("if x > 10:"),
      code("    print('Больше 10')"),
      code("elif x == 10:"),
      code("    print('Равно 10')"),
      code("else:"),
      code("    print('Меньше 10')"),
      spacer(),

      h3("Циклы"),
      code("for i in range(5):      # 0, 1, 2, 3, 4"),
      code("    print(i)"),
      spacer(),
      code("for item in ['a', 'b', 'c']:"),
      code("    print(item)"),
      spacer(),
      code("while condition:"),
      code("    do_something()"),
      note("Разница for vs while: for используем когда знаем количество итераций, while — когда условие остановки заранее неизвестно."),
      spacer(),

      h2("4.4 Функции"),
      code("def calculate_ltv(revenue, churn_rate):"),
      code("    \"\"\"Рассчитать LTV\"\"\""),
      code("    if churn_rate == 0:"),
      code("        return None"),
      code("    return revenue / churn_rate"),
      spacer(),
      code("# Значения по умолчанию"),
      code("def greet(name, greeting='Привет'):"),
      code("    return f'{greeting}, {name}!'"),
      spacer(),
      code("# lambda — анонимная функция"),
      code("double = lambda x: x * 2"),
      code("sorted_list = sorted(data, key=lambda x: x['date'])"),
      spacer(),

      h2("4.5 Включения (Comprehensions)"),
      code("# List comprehension — список чётных квадратов"),
      code("squares = [x**2 for x in range(10) if x % 2 == 0]"),
      spacer(),
      code("# Dict comprehension — частоты слов"),
      code("text = ['кот', 'кот', 'пёс', 'кот', 'пёс']"),
      code("freq = {word: text.count(word) for word in set(text)}"),
      spacer(),

      h2("4.6 Обработка ошибок"),
      code("try:"),
      code("    result = 10 / x"),
      code("except ZeroDivisionError:"),
      code("    result = None"),
      code("except Exception as e:"),
      code("    print(f'Ошибка: {e}')"),
      code("finally:"),
      code("    print('Блок выполнится всегда')"),
      spacer(),

      h2("4.7 Работа со строками"),
      code("s = '  Hello, World!  '"),
      code("s.strip()          # убрать пробелы по краям: 'Hello, World!'"),
      code("s.lower()          # в нижний регистр"),
      code("s.upper()          # в верхний регистр"),
      code("s.replace(',', '') # заменить символ"),
      code("s.split(', ')      # разбить: ['Hello', 'World!']"),
      code("', '.join(['a','b','c'])  # собрать: 'a, b, c'"),
      spacer(),

      h2("4.8 Работа с датами"),
      code("from datetime import datetime, timedelta"),
      spacer(),
      code("today = datetime.now()"),
      code("date_str = '2024-01-15'"),
      code("date = datetime.strptime(date_str, '%Y-%m-%d')"),
      code("date + timedelta(days=7)   # через неделю"),
      code("(date2 - date1).days       # разница в днях"),
      spacer(),

      h2("4.9 Pandas — основа работы с данными"),
      h3("Чтение данных"),
      code("import pandas as pd"),
      code("df = pd.read_csv('data.csv')"),
      code("df = pd.read_excel('data.xlsx', sheet_name='Sheet1')"),
      code("df = pd.read_json('data.json')"),
      spacer(),

      h3("Первичный осмотр"),
      code("df.head(5)          # первые 5 строк"),
      code("df.tail(5)          # последние 5 строк"),
      code("df.shape            # (строки, столбцы)"),
      code("df.info()           # типы данных, null-значения"),
      code("df.describe()       # статистики для числовых столбцов"),
      code("df['col'].value_counts()  # частоты значений"),
      spacer(),

      h3("Индексация и фильтрация"),
      code("df['col']                    # столбец"),
      code("df[['col1', 'col2']]         # несколько столбцов"),
      code("df.loc[0:5, 'col1':'col3']   # по меткам"),
      code("df.iloc[0:5, 0:3]            # по позициям"),
      code("df[df['age'] > 25]           # фильтр по условию"),
      code("df[df['city'].isin(['Москва', 'СПб'])]"),
      code("df.query('age > 25 and city == \"Москва\"')"),
      spacer(),

      h3("Работа с пропущенными значениями"),
      code("df.isnull().sum()            # количество null по столбцам"),
      code("df.dropna()                  # удалить строки с null"),
      code("df.fillna(0)                 # заполнить нулями"),
      code("df['col'].fillna(df['col'].median())  # медианой"),
      spacer(),

      h3("Операции со столбцами"),
      code("df['new_col'] = df['col1'] * 2              # создать столбец"),
      code("df.drop(columns=['col1', 'col2'])            # удалить"),
      code("df.rename(columns={'old': 'new'})           # переименовать"),
      code("df['col'] = df['col'].astype('float')       # изменить тип"),
      spacer(),

      h3("Группировка и агрегация"),
      code("df.groupby('city')['revenue'].sum()"),
      code("df.groupby('city').agg({'revenue': 'sum', 'orders': 'count'})"),
      code("df.groupby(['city', 'category'])['revenue'].agg(['sum', 'mean', 'count'])"),
      spacer(),

      h3("Объединение таблиц"),
      code("# merge — как JOIN в SQL"),
      code("pd.merge(df1, df2, on='user_id', how='left')"),
      code("pd.merge(df1, df2, left_on='id', right_on='user_id')"),
      spacer(),
      code("# concat — склеить таблицы"),
      code("pd.concat([df1, df2], ignore_index=True)           # по строкам"),
      code("pd.concat([df1, df2], axis=1)                      # по столбцам"),
      spacer(),

      h3("Работа с датами"),
      code("df['date'] = pd.to_datetime(df['date'])"),
      code("df['year'] = df['date'].dt.year"),
      code("df['month'] = df['date'].dt.month"),
      code("df['week'] = df['date'].dt.isocalendar().week"),
      spacer(),

      h3("Работа со строками"),
      code("df['col'].str.lower()"),
      code("df['col'].str.contains('keyword', na=False)"),
      code("df['col'].str.replace('old', 'new')"),
      code("df['col'].str.split(',').str[0]"),
      spacer(),

      h3("Pivot table"),
      code("df.pivot_table(values='revenue', index='region', columns='category', aggfunc='sum', fill_value=0)"),
      spacer(),

      h3("Apply и Map"),
      code("# apply — применить функцию к каждой строке/столбцу (медленнее векторных операций)"),
      code("df['new'] = df['col'].apply(lambda x: x * 2)"),
      spacer(),
      code("# map — замена значений через словарь"),
      code("df['status'] = df['status_code'].map({1: 'active', 0: 'inactive'})"),
      note("apply работает медленно на больших данных. Всегда предпочитайте векторные операции Pandas (df['col'] * 2) вместо apply где возможно."),
      spacer(),

      h2("4.10 Визуализация (Matplotlib / Seaborn)"),
      code("import matplotlib.pyplot as plt"),
      code("import seaborn as sns"),
      spacer(),
      code("# Гистограмма"),
      code("df['age'].hist(bins=20)"),
      code("plt.show()"),
      spacer(),
      code("# Линейный график"),
      code("df.plot(x='date', y='revenue', kind='line')"),
      spacer(),
      code("# Scatter plot"),
      code("sns.scatterplot(data=df, x='age', y='revenue')"),
      spacer(),
      code("# Boxplot"),
      code("sns.boxplot(data=df, x='category', y='revenue')"),
      spacer(),

      pageBreak(),

      // ══════════════════════════════════════════════════════════════════════════
      // 5. СТАТИСТИКА
      // ══════════════════════════════════════════════════════════════════════════
      h1("5. СТАТИСТИКА И ТЕОРИЯ ВЕРОЯТНОСТЕЙ"),
      spacer(),

      h2("5.1 Теория вероятностей — основы"),
      h3("Определение вероятности"),
      p("Классическое: P(A) = число благоприятных исходов / общее число исходов. Статистическое: P(A) = частота события при большом числе испытаний."),
      spacer(),

      h3("Основные формулы"),
      bullet("Сложение (несовместные события): P(A или B) = P(A) + P(B)"),
      bullet("Сложение (совместные события): P(A или B) = P(A) + P(B) - P(A и B)"),
      bullet("Умножение (независимые): P(A и B) = P(A) × P(B)"),
      bullet("Умножение (зависимые): P(A и B) = P(A) × P(B|A)"),
      bullet("Условная вероятность: P(A|B) = P(A и B) / P(B)"),
      spacer(),

      h3("Теорема Байеса"),
      p("P(A|B) = P(B|A) × P(A) / P(B)"),
      p("Позволяет обновлять вероятность гипотезы на основе новых данных. Классический пример — тест на болезнь: если тест положительный, какова реальная вероятность болезни с учётом её распространённости?"),
      tip("Типичный вопрос на собеседовании: «Тест на болезнь даёт ложноположительный результат в 5% случаев. Болезнь встречается у 1% людей. Тест положительный. Какова вероятность болезни?» — ответ через теорему Байеса (~17%, а не 95%)."),
      spacer(),

      h3("Математическое ожидание и дисперсия"),
      bullet("E[X] — математическое ожидание: средневзвешенное значение случайной величины"),
      bullet("Var[X] = E[(X - E[X])²] — дисперсия: средний квадрат отклонения от среднего"),
      bullet("std(X) = √Var[X] — стандартное отклонение"),
      spacer(),

      h3("Ковариация и корреляция"),
      bullet("Ковариация — мера совместного изменения двух переменных (зависит от масштаба)"),
      bullet("Корреляция Пирсона — ковариация, нормированная на произведение стандартных отклонений. От -1 до 1. Для линейных зависимостей."),
      bullet("Корреляция Спирмена — ранговая корреляция. Устойчива к выбросам. Для монотонных зависимостей."),
      note("Корреляция ≠ причинно-следственная связь. Это важно подчеркнуть на собеседовании."),
      spacer(),

      h3("Ключевые распределения"),
      bullet("Нормальное (Гауссово) — симметричное, колоколообразное. Применяется везде по ЦПТ."),
      bullet("Биномиальное — количество успехов в n независимых испытаниях с вероятностью p."),
      bullet("Пуассоновское — количество редких событий за единицу времени. Пример: количество звонков в call-центр за час."),
      bullet("Бернулли — частный случай биномиального при n=1 (монета)."),
      bullet("Равномерное — все значения на отрезке [a,b] равновероятны."),
      bullet("Бета-распределение — используется в байесовских A/B тестах для моделирования конверсий."),
      spacer(),

      h3("Центральная предельная теорема (ЦПТ)"),
      p("Сумма (или среднее) большого числа независимых одинаково распределённых случайных величин стремится к нормальному распределению — независимо от исходного распределения. Именно поэтому нормальное распределение встречается повсюду, и именно поэтому статистические тесты работают."),
      tip("ЦПТ — это основа A/B тестирования: при достаточно большой выборке среднее любой метрики распределено нормально, что позволяет применять z-тест и t-тест."),
      spacer(),

      h2("5.2 Статистика — основы"),
      h3("Генеральная совокупность vs выборка"),
      p("Генеральная совокупность — все возможные объекты. Выборка — подмножество, по которому оцениваем параметры генеральной совокупности. Смещённая выборка приводит к ошибочным выводам."),
      spacer(),

      h3("Точечные оценки"),
      bullet("Выборочное среднее x̄ — несмещённая оценка математического ожидания"),
      bullet("Выборочная дисперсия s² = Σ(xi - x̄)² / (n-1) — делим на n-1 (поправка Бесселя)"),
      bullet("Стандартное отклонение s = √s²"),
      spacer(),

      h3("Доверительный интервал (ДИ)"),
      p("95% ДИ означает: если мы много раз повторим эксперимент, в 95% случаев построенный интервал будет содержать истинное значение параметра. Это НЕ означает, что истинное значение с 95% вероятностью лежит в данном интервале."),
      p("Формула для среднего: x̄ ± z × (σ / √n), где z = 1.96 для 95% ДИ."),
      note("Ловушка на собеседовании: «Правда ли, что с 95% вероятностью истинное значение лежит в 95% ДИ?» — нет. Истинное значение фиксировано, ДИ случаен."),
      spacer(),

      h3("Гипотезы и p-value"),
      pMixed([run("Нулевая гипотеза (H0) ", { bold: true }), run("— нет эффекта, нет разницы. Пример: конверсия в контроле = конверсии в тесте.")]),
      pMixed([run("Альтернативная гипотеза (H1) ", { bold: true }), run("— есть эффект. Пример: конверсия в тесте > конверсии в контроле.")]),
      spacer(),
      pMixed([run("p-value ", { bold: true }), run("— вероятность наблюдать данные, настолько же или более экстремальные, чем наши, при условии что H0 верна. p-value — НЕ вероятность того, что H0 верна.")]),
      bullet("p < α (уровень значимости, обычно 0.05) → отвергаем H0"),
      bullet("p ≥ α → недостаточно данных, чтобы отвергнуть H0"),
      spacer(),

      h3("Ошибки первого и второго рода"),
      bullet("Ошибка первого рода (α, ложноположительная) — H0 верна, но мы её отвергли (нашли эффект там, где его нет)"),
      bullet("Ошибка второго рода (β, ложноотрицательная) — H0 неверна, но мы её не отвергли (пропустили реальный эффект)"),
      bullet("Мощность теста = 1 - β — вероятность обнаружить эффект, если он есть (обычно желают ≥ 0.8)"),
      spacer(),

      h2("5.3 Статистические тесты"),
      h3("Z-тест и t-тест"),
      pMixed([run("Z-тест ", { bold: true }), run("— для больших выборок (n > 30) или когда известна генеральная дисперсия.")]),
      pMixed([run("t-тест (Стьюдента) ", { bold: true }), run("— для малых выборок, когда дисперсия неизвестна. Бывает одновыборочный, двухвыборочный и парный.")]),
      spacer(),

      h3("Критерий хи-квадрат (χ²)"),
      p("Применяется для сравнения категориальных переменных. Пример: изменилось ли распределение пользователей по сегментам после изменения алгоритма? Или: зависит ли платная подписка от страны?"),
      spacer(),

      h3("Размер выборки"),
      p("Минимальный размер выборки зависит от: уровня значимости α, мощности 1-β, ожидаемого эффекта (MDE — минимальный определяемый эффект) и дисперсии метрики. Чем меньше эффект, тем больше нужна выборка."),
      spacer(),

      h3("Проверка на нормальность"),
      bullet("Визуально: QQ-plot, гистограмма"),
      bullet("Критерий Шапиро-Уилка (для малых выборок)"),
      bullet("Критерий Колмогорова-Смирнова"),
      spacer(),

      h3("Непараметрические тесты"),
      p("Применяются когда данные не нормально распределены или выборки малы. Примеры: критерий Манна-Уитни (аналог t-теста для ненормальных данных), критерий Вилкоксона."),
      spacer(),

      h2("5.4 Продвинутые темы"),
      h3("Bootstrap"),
      p("Метод многократной повторной выборки с возвращением из имеющихся данных. Позволяет оценить распределение любой статистики без предположений о нормальности. Мощный инструмент для A/B тестирования ненормальных метрик (выручка с хвостами, LTV)."),
      spacer(),

      h3("Множественное тестирование"),
      p("Если провести 20 тестов при α=0.05, ожидается 1 ложноположительный результат просто по случайности. Поправки: поправка Бонферрони (делим α на количество тестов, консервативная) и метод Бенджамини-Хохберга (контроль FDR, менее консервативная)."),
      spacer(),

      h3("Delta-метод"),
      p("Техника для расчёта дисперсии отношений метрик (например, средний чек = выручка / заказы). Позволяет правильно применять t-тест к таким метрикам."),
      spacer(),

      pageBreak(),

      // ══════════════════════════════════════════════════════════════════════════
      // 6. A/B ТЕСТИРОВАНИЕ
      // ══════════════════════════════════════════════════════════════════════════
      h1("6. A/B ТЕСТИРОВАНИЕ"),
      spacer(),

      h2("6.1 Что такое A/B тест"),
      p("A/B тест — контролируемый эксперимент, в котором пользователи случайно разделяются на группы: контрольную (A, старый вариант) и тестовую (B, новый вариант). Цель — определить, привело ли изменение к статистически значимому улучшению целевой метрики."),
      spacer(),

      h2("6.2 Процесс проведения теста"),
      bullet("Формулируем гипотезу: что меняем и почему ожидаем улучшение"),
      bullet("Выбираем целевую метрику и guardrail-метрики (метрики, которые не должны ухудшиться)"),
      bullet("Рассчитываем необходимый размер выборки (MDE, α, мощность)"),
      bullet("Рандомизируем пользователей по группам"),
      bullet("Ждём достаточного накопления данных (не смотрим постоянно — проблема peeking)"),
      bullet("Проводим статистический тест, интерпретируем результат"),
      bullet("Принимаем решение и запускаем (или не запускаем) изменение"),
      spacer(),

      h2("6.3 Частые ошибки в A/B тестах"),
      bullet("Peeking problem — остановка теста, когда увидели значимость. Нужно заранее определить размер выборки и ждать его достижения"),
      bullet("Нарушение SUTVA — один пользователь влияет на другого (например, реферальная программа)"),
      bullet("Новизны эффект — пользователи реагируют на изменение только потому, что оно новое"),
      bullet("Множественное тестирование — проверять много гипотез без коррекции"),
      spacer(),

      h2("6.4 Виды тестов"),
      bullet("A/B тест — два варианта (контроль и тест)"),
      bullet("A/A тест — проверка корректности сплитования: обе группы видят одно и то же, значимых различий быть не должно"),
      bullet("A/B/C тест — несколько вариантов одновременно, требует поправки на множественность"),
      spacer(),

      pageBreak(),

      // ══════════════════════════════════════════════════════════════════════════
      // 7. ПРОДУКТОВАЯ АНАЛИТИКА
      // ══════════════════════════════════════════════════════════════════════════
      h1("7. ПРОДУКТОВАЯ АНАЛИТИКА И МЕТРИКИ"),
      spacer(),

      h2("7.1 Ключевые метрики"),
      h3("Активность пользователей"),
      bullet("DAU (Daily Active Users) — количество уникальных активных пользователей в день"),
      bullet("MAU (Monthly Active Users) — за месяц"),
      bullet("DAU/MAU ratio (Stickiness) — насколько ежедневно активны месячные пользователи. Высокий показатель ≈ высокая вовлечённость."),
      spacer(),

      h3("Монетизация"),
      bullet("ARPU (Average Revenue Per User) = Выручка / Количество пользователей"),
      bullet("ARPPU (Average Revenue Per Paying User) = Выручка / Количество платящих пользователей"),
      bullet("LTV (Lifetime Value) — сколько денег приносит пользователь за всё время ≈ ARPU / Churn Rate"),
      bullet("CAC (Customer Acquisition Cost) — стоимость привлечения одного клиента"),
      bullet("LTV/CAC > 3 — здоровое соотношение"),
      spacer(),

      h3("Удержание и отток"),
      bullet("Retention Rate (N-day) — доля пользователей, вернувшихся на N-й день после первого визита"),
      bullet("Rolling Retention — доля пользователей, использовавших продукт хотя бы один раз после дня N"),
      bullet("Churn Rate — доля пользователей, переставших использовать продукт за период. Churn = 1 - Retention"),
      spacer(),

      h2("7.2 Воронки конверсии"),
      p("Воронка — последовательность шагов от первого контакта до целевого действия. Анализируем конверсию на каждом шаге: сколько пользователей переходит к следующему шагу и где теряем больше всего."),
      tip("Пример вопроса: «Как вы построите воронку для онбординга нового пользователя?» — опишите шаги, метрики конверсии каждого шага, сегментацию по источникам трафика."),
      spacer(),

      h2("7.3 Когортный анализ"),
      p("Когорта — группа пользователей, объединённых общим признаком во времени (обычно месяц регистрации). Когортный анализ позволяет сравнивать поведение пользователей с одинаковым «возрастом» и отслеживать изменения качества привлечения со временем."),
      spacer(),

      h2("7.4 Фреймворки и стратегические инструменты"),
      h3("AARRR (Pirate Metrics)"),
      bullet("Acquisition — откуда приходят пользователи"),
      bullet("Activation — первый позитивный опыт"),
      bullet("Retention — возвращаются ли"),
      bullet("Revenue — платят ли"),
      bullet("Referral — рекомендуют ли"),
      spacer(),

      h3("HEART (Google)"),
      bullet("Happiness — насколько довольны пользователи (NPS, CSAT)"),
      bullet("Engagement — глубина взаимодействия"),
      bullet("Adoption — принятие новых функций"),
      bullet("Retention — удержание"),
      bullet("Task Success — успешность выполнения задач"),
      spacer(),

      h3("North Star Metric"),
      p("Одна ключевая метрика, отражающая ценность продукта для пользователей и бизнеса одновременно. Примеры: Spotify — время прослушивания, Airbnb — ночи бронирования, Uber — количество завершённых поездок."),
      spacer(),

      h3("Дерево метрик"),
      p("Иерархическое разложение ключевой метрики на составляющие. Пример: Выручка = DAU × ARPU. DAU = Новые пользователи + Вернувшиеся пользователи. ARPU = Доля платящих × ARPPU."),
      spacer(),

      h2("7.5 Диагностика падения метрики"),
      p("Когда метрика падает, задайте себе эти вопросы последовательно:"),
      bullet("Это реальное падение или ошибка в данных? (Проверьте пайплайн, логи, отслеживание событий)"),
      bullet("Когда началось? Коррелирует ли с деплоем, маркетинговой кампанией, внешним событием?"),
      bullet("Падение равномерное по всем сегментам или только в одном? (Платформа, страна, источник)"),
      bullet("Какие смежные метрики изменились? (Если падает конверсия, вырос ли трафик?)"),
      bullet("Есть ли внешние причины? (Сезонность, праздники, действия конкурентов)"),
      tip("Это один из самых частых кейсов на собеседовании. Структурируйте ответ: данные → сегментация → гипотезы → проверка."),
      spacer(),

      pageBreak(),

      // ══════════════════════════════════════════════════════════════════════════
      // 8. АЛГОРИТМЫ
      // ══════════════════════════════════════════════════════════════════════════
      h1("8. АЛГОРИТМЫ И СТРУКТУРЫ ДАННЫХ"),
      spacer(),

      h2("8.1 Сложность алгоритма (Big O)"),
      bullet("O(1) — константная: доступ к элементу массива по индексу"),
      bullet("O(log n) — логарифмическая: бинарный поиск"),
      bullet("O(n) — линейная: проход по массиву"),
      bullet("O(n log n) — эффективная сортировка (mergesort, quicksort)"),
      bullet("O(n²) — квадратичная: вложенные циклы, пузырьковая сортировка"),
      note("На собеседовании достаточно знать концепцию и уметь оценить сложность написанного кода."),
      spacer(),

      h2("8.2 Ключевые структуры данных"),
      bullet("Массивы/списки — O(1) доступ по индексу, O(n) поиск"),
      bullet("Хеш-таблицы (dict в Python) — O(1) вставка, поиск, удаление в среднем"),
      bullet("Стек (LIFO) — последний пришёл, первый вышел. Пример: история браузера"),
      bullet("Очередь (FIFO) — первый пришёл, первый вышел. Пример: очередь задач"),
      spacer(),

      h2("8.3 Популярные задачи"),
      bullet("Алгоритм Евклида (НОД): gcd(a, b) = gcd(b, a % b) пока b != 0"),
      bullet("Бинарный поиск: ищем в отсортированном массиве за O(log n)"),
      bullet("Два указателя: для задач на массивы и строки (разворот, палиндром)"),
      bullet("Рекурсия: функция вызывает саму себя. Нужна база рекурсии для остановки"),
      spacer(),

      h2("8.4 Типичные задачи на аналитических собеседованиях"),
      code("# Найти два числа с суммой target"),
      code("def two_sum(nums, target):"),
      code("    seen = {}"),
      code("    for i, n in enumerate(nums):"),
      code("        diff = target - n"),
      code("        if diff in seen:"),
      code("            return [seen[diff], i]"),
      code("        seen[n] = i"),
      spacer(),
      code("# Подсчёт частот элементов"),
      code("from collections import Counter"),
      code("freq = Counter(['a', 'b', 'a', 'c', 'b', 'a'])"),
      code("# Counter({'a': 3, 'b': 2, 'c': 1})"),
      spacer(),

      pageBreak(),

      // ══════════════════════════════════════════════════════════════════════════
      // 9. БИЗНЕС-АНАЛИТИКА
      // ══════════════════════════════════════════════════════════════════════════
      h1("9. БИЗНЕС-АНАЛИТИКА"),
      spacer(),

      h2("9.1 Ключевые понятия"),
      bullet("BPMN — нотация для описания бизнес-процессов в виде диаграмм"),
      bullet("UML — унифицированный язык моделирования (диаграммы классов, последовательностей, use case)"),
      bullet("User Story — описание функциональности с точки зрения пользователя: «Как [роль], я хочу [действие], чтобы [ценность]»"),
      bullet("Use Case — описание взаимодействия актора с системой для достижения цели"),
      spacer(),

      h2("9.2 Требования"),
      pMixed([run("Функциональные требования ", { bold: true }), run("— что система должна делать (функции, поведение).")]),
      pMixed([run("Нефункциональные требования ", { bold: true }), run("— как система должна это делать (производительность, безопасность, масштабируемость).")]),
      spacer(),

      h2("9.3 Управление продуктом"),
      bullet("Бэклог — упорядоченный список задач и требований к продукту"),
      bullet("Scrum — итеративный фреймворк разработки. Спринт = 1-4 недели. Артефакты: Product Backlog, Sprint Backlog, Increment"),
      bullet("Agile — философия гибкой разработки, ценящая людей и взаимодействие, работающий продукт, сотрудничество с заказчиком"),
      bullet("Jira — система управления задачами и проектами"),
      bullet("Confluence — вики для документации и совместной работы"),
      spacer(),

      pageBreak(),

      // ══════════════════════════════════════════════════════════════════════════
      // 10. ВОПРОСЫ С СОБЕСЕДОВАНИЙ
      // ══════════════════════════════════════════════════════════════════════════
      h1("10. ТИПИЧНЫЕ ВОПРОСЫ НА СОБЕСЕДОВАНИИ"),
      spacer(),

      h2("SQL"),
      bullet("Объясните разницу между LEFT JOIN и INNER JOIN — и когда что использовать"),
      bullet("Что такое оконные функции? Чем отличаются от GROUP BY?"),
      bullet("В чём разница между RANK() и DENSE_RANK()?"),
      bullet("Как найти дубликаты в таблице?"),
      bullet("Как найти N-е максимальное значение?"),
      bullet("Что такое CTE и зачем использовать WITH?"),
      bullet("Порядок выполнения SQL запроса — почему нельзя использовать алиас из SELECT в WHERE?"),
      spacer(),

      h2("Статистика и A/B тесты"),
      bullet("Что такое p-value? (Ожидают точное определение, не интуитивное)"),
      bullet("В чём разница между ошибкой первого и второго рода?"),
      bullet("Что такое доверительный интервал? Правда ли, что с 95% вероятностью истинное значение в 95% ДИ?"),
      bullet("Как рассчитать необходимый размер выборки для A/B теста?"),
      bullet("Что делать, если данные не нормально распределены?"),
      bullet("Как бороться с проблемой множественных сравнений?"),
      bullet("Объясните теорему Байеса на простом примере"),
      bullet("Что такое ЦПТ и почему она важна для A/B тестов?"),
      spacer(),

      h2("Продуктовые кейсы"),
      bullet("«Метрика X упала на 20% вчера. Ваши действия?»"),
      bullet("«Придумайте метрики для новой кнопки / экрана онбординга / реферальной программы»"),
      bullet("«Как вы измерите успех этого продуктового изменения?»"),
      bullet("«Что такое North Star Metric? Какая она у Spotify / Airbnb?»"),
      bullet("«Как построить дерево метрик для MAU?»"),
      bullet("«Что такое LTV и как его считать?»"),
      bullet("«Retention упал — что проверите в первую очередь?»"),
      spacer(),

      h2("Python / Pandas"),
      bullet("Чем отличается loc от iloc?"),
      bullet("Как объединить две таблицы и посчитать метрику?"),
      bullet("Как вы обрабатываете пропущенные значения? Какая стратегия зависит от типа пропусков?"),
      bullet("Как найти топ-10 пользователей по выручке за последние 30 дней?"),
      bullet("Почему apply работает медленно?"),
      spacer(),

      h2("HR и soft skills"),
      bullet("«Расскажите о себе» — 3-4 минуты, структурированно, с акцентом на аналитику"),
      bullet("«В чём разница между продуктовым аналитиком и аналитиком данных?»"),
      bullet("«Расскажите о самом значимом достижении» — используйте STAR (Situation, Task, Action, Result)"),
      bullet("«Расскажите о ситуации, когда вы ошиблись» — покажите рефлексию и рост"),
      bullet("«Почему мы должны выбрать именно вас?» — подготовьте чёткий ответ"),
      spacer(),

      pageBreak(),

      // ══════════════════════════════════════════════════════════════════════════
      // 11. ПРИОРИТЕТЫ ПОДГОТОВКИ
      // ══════════════════════════════════════════════════════════════════════════
      h1("11. ПРИОРИТЕТЫ ПОДГОТОВКИ (по роадмапу)"),
      spacer(),
      p("На основе роадмапа — что проверяют чаще всего:"),
      spacer(),

      h2("Высокий приоритет (учить в первую очередь)"),
      bullet("SQL — оконные функции, джойны, GROUP BY, порядок выполнения запроса"),
      bullet("Python + Pandas — базовые структуры данных, groupby, merge, работа с пропусками"),
      bullet("Статистика — p-value, ДИ, ошибки I/II рода, размер выборки, MDE"),
      bullet("Теория вероятностей — условная вероятность, теорема Байеса, ЦПТ, основные распределения"),
      bullet("Кейсы — диагностика падения метрики, построение дерева метрик, придумать метрики для фичи"),
      bullet("Структурное мышление — принцип пирамиды Минто, структура ответа на кейс"),
      spacer(),

      h2("Средний приоритет"),
      bullet("Excel — ВПР, ИНДЕКС+ПОИСКПОЗ, Pivot Table, СУММЕСЛИМН, СЧЁТЕСЛИМН"),
      bullet("Визуализация данных — виды графиков, когда что использовать, Datalens"),
      bullet("Эконометрика — регрессия, базовое понимание Treatment Effect"),
      spacer(),

      h2("Низкий приоритет"),
      bullet("Алгоритмы — BigO, массивы, хеш-таблицы, бинарный поиск, рекурсия"),
      bullet("Машинное обучение — линейная/логистическая регрессия, деревья решений, случайный лес"),
      spacer(),

   
